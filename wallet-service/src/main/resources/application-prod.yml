spring:
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true

tokenrealty:
  wallet:
    encryption:
      mode: kms
      kms-key-id: ${AWS_KMS_KEY_ID:alias/tokenrealty-wallet}

logging:
  level:
    com.tokenrealty: INFO
