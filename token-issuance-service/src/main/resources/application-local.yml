tokenrealty:
  issuance:
    seed-dev-token-contract: true
    # Optional: set DIVIDEND_DISTRIBUTOR_ADDRESS after `npm run deploy:local` in hardhat/
    # dividend-distributor-address: ${DIVIDEND_DISTRIBUTOR_ADDRESS:}
  dividend:
    rent-collected-listener-enabled: false
  kafka:
    enabled: true
    dlq:
      enabled: true

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

blockchain:
  network: local
  rpc-url: http://localhost:8545
  chain-id: 31337
